---
import BaseLayout from './BaseLayout.astro';
const { title = 'Meraki – Admin' } = Astro.props;
---
<BaseLayout title={title}>
    <script is:inline>
        if (!sessionStorage.getItem('admin_token')) {
            window.location.replace('/login');
        }
    </script>
    <slot />
</BaseLayout>
